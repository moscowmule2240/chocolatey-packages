name: Update antigravity-ide

# Updates the antigravity-ide package. The job is in _update-package.yml.

on:
  workflow_dispatch: {}        # manual run
  # Every 5 minutes (GitHub's minimum interval), starting at minute 0. Each
  # update-*.yml starts at a different minute, 0-4, so the package workflows do
  # not all start in the same minute. Scheduled runs are best-effort and may be
  # delayed under load. A run only pushes when upstream has a version newer than
  # the nuspec.
  schedule:
    - cron: "0-59/5 * * * *"   # every 5 minutes from minute 0

permissions:
  contents: write              # the job commits the version bump back

concurrency:
  group: update-antigravity-ide
  cancel-in-progress: false

jobs:
  update:
    uses: ./.github/workflows/_update-package.yml
    with:
      package: antigravity-ide
    secrets:
      CHOCO_API_KEY: ${{ secrets.CHOCO_API_KEY }}
